@model IEnumerable<Brodheim.Models.QuemSomos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameRepresentative)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuantityRepresentative)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageRepresentative)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameRepresentative)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantityRepresentative)
            </td>
            <td>
                    <img src="@item.ImageRepresentative" alt="Slider Image" class="img-fluid" style="max-width: 200px;" />
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
